spring.application.name=SystemRestaurant

# PostgreSQL Database Configuration - RENDER
spring.datasource.url=jdbc:postgresql://dpg-d4u1fa6uk2gs73c9o4pg-a:5432/restaurante_db_vj0c
spring.datasource.username=restaurante_user
spring.datasource.password=dtp3VPTb5H3HKphuUL67VkqInAIPVMeD
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=never

# Server Configuration
server.port=${PORT:8080}

# Disable ticket service (Node.js)
ticket.service.url=http://localhost:3000

# Async Configuration
spring.mvc.async.request-timeout=30000
jdbc:mysql://<tidb-host>:4000/restauranteDB?user=<SebGXSRyM1XERQ3.root>&password=<bLVLNL4d2zvLiayw>&useSSL=true&requireSSL=true&autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&tlsVersions=TLSv1.2,TLSv1.3

# Backup Configuration (deshabilitado en producción)
backup.enabled=false
backup.directory=backups/
backup.retention.days=30

# Maintenance Configuration
maintenance.cleanup.enabled=false
maintenance.monitoring.enabled=true
maintenance.log.retention.days=30

# Scheduled Tasks
spring.task.scheduling.pool.size=5

# Actuator Endpoints
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Logging
logging.level.root=INFO
logging.level.com.app.SystemRestaurant=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN